# Releases

Run `npm run changeset` for user-visible changes. Choose a patch, minor, or major
release and describe the resulting behavior. The release workflow opens a version
pull request; merging it builds and publishes the package.
